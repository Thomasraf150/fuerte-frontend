/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost'], // Add your domain here (e.g., localhost for local development)
  },
};

export default nextConfig;
